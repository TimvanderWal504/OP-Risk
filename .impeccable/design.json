{
  "schemaVersion": 2,
  "generatedAt": "2026-09-25T08:46:24.242Z",
  "title": "Design System: Operatie Atlas",
  "extensions": {
    "colorMeta": {
      "territory-green": { "role": "primary", "displayName": "Territory Green", "canonical": "#a1c23a", "tonalRamp": ["#1b2709", "#36481a", "#41571a", "#536f1b", "#6b8f1f", "#84ad28", "#a1c23a", "#bbd45f"] },
      "territory-green-fill": { "role": "primary", "displayName": "Territory Green Fill", "canonical": "#84ad28", "tonalRamp": ["#1b2709", "#36481a", "#41571a", "#536f1b", "#6b8f1f", "#84ad28", "#a1c23a", "#bbd45f"] },
      "command-blue": { "role": "secondary", "displayName": "Command Blue", "canonical": "#6ba2d8", "tonalRamp": ["#0b1a2b", "#142b44", "#173351", "#193d62", "#1c4a78", "#215990", "#3d79b5", "#6ba2d8"] },
      "command-blue-deep": { "role": "secondary", "displayName": "Command Blue Deep", "canonical": "#2e6aa8", "tonalRamp": ["#0b1a2b", "#142b44", "#173351", "#193d62", "#1c4a78", "#215990", "#3d79b5", "#6ba2d8"] },
      "recon-silver": { "role": "tertiary", "displayName": "Recon Silver", "canonical": "#c2cddd", "tonalRamp": ["#212733", "#394457", "#425068", "#4f607d", "#627798", "#7b93b3", "#9cb0ca", "#c2cddd"] },
      "recon-silver-deep": { "role": "tertiary", "displayName": "Recon Silver Deep", "canonical": "#627798", "tonalRamp": ["#212733", "#394457", "#425068", "#4f607d", "#627798", "#7b93b3", "#9cb0ca", "#c2cddd"] },
      "alert-red": { "role": "status", "displayName": "Alert Red", "canonical": "#ff4d52" },
      "conquest-green": { "role": "status", "displayName": "Conquest Green", "canonical": "#56c96a" },
      "caution-amber": { "role": "status", "displayName": "Caution Amber", "canonical": "#f2c14e" },
      "field-ink-950": { "role": "neutral", "displayName": "Field Ink 950 (canvas)", "canonical": "#080c14", "tonalRamp": ["#080c14", "#111927", "#141d2c", "#1b2738", "#243246", "#354054", "#475468", "#6f7e97"] },
      "field-ink-800": { "role": "neutral", "displayName": "Field Ink 800 (surface)", "canonical": "#1b2738" },
      "field-ink-700": { "role": "neutral", "displayName": "Field Ink 700 (surface-3)", "canonical": "#243246" },
      "field-ink-400": { "role": "neutral", "displayName": "Field Ink 400 (muted text)", "canonical": "#6f7e97" },
      "field-ink-100": { "role": "neutral", "displayName": "Field Ink 100 (primary text)", "canonical": "#eef2f8" },
      "glass-surface-base-opaque": { "role": "glass-fallback", "displayName": "Glass Surface Base (opaque fallback only)", "canonical": "#1b2738", "note": "Fully opaque fallback, used only when backdrop-filter cannot run (unsupported browser, prefers-reduced-transparency). The normal case uses the translucent glass-surface-* tint of the same tier." },
      "glass-surface-raised-opaque": { "role": "glass-fallback", "displayName": "Glass Surface Raised (opaque fallback only)", "canonical": "#243246", "note": "Fully opaque fallback, used only when backdrop-filter cannot run (unsupported browser, prefers-reduced-transparency). The normal case uses the translucent glass-surface-* tint of the same tier." },
      "glass-surface-overlay-opaque": { "role": "glass-fallback", "displayName": "Glass Surface Overlay (opaque fallback only)", "canonical": "#243246", "note": "Fully opaque fallback, used only when backdrop-filter cannot run (unsupported browser, prefers-reduced-transparency). The normal case uses the translucent glass-surface-* tint of the same tier." },
      "glass-surface-recessed-opaque": { "role": "glass-fallback", "displayName": "Glass Surface Recessed (opaque fallback only)", "canonical": "#080c14", "note": "Fully opaque fallback, used only when backdrop-filter cannot run (unsupported browser, prefers-reduced-transparency). The normal case uses the translucent glass-surface-* tint of the same tier." },
      "glass-border": { "role": "glass", "displayName": "Glass Border", "canonical": "rgba(255, 255, 255, 0.14)" },
      "glass-fg": { "role": "glass-text", "displayName": "Glass Text Primary", "canonical": "#eef2f8" },
      "glass-fg-secondary": { "role": "glass-text", "displayName": "Glass Text Secondary", "canonical": "color-mix(in srgb, #eef2f8 90%, transparent)" },
      "glass-fg-muted": { "role": "glass-text", "displayName": "Glass Text Muted", "canonical": "color-mix(in srgb, #eef2f8 75%, transparent)" },
      "glass-surface-base": { "role": "glass-surface", "displayName": "Glass Surface Base", "canonical": "rgba(20, 29, 44, 0.72)", "note": "Translucent Field Ink tint per elevation tier (since 2026-09-22; before that GlassPanel was clear glass). On the TV the alpha follows the host panel-opacity setting." },
      "glass-surface-raised": { "role": "glass-surface", "displayName": "Glass Surface Raised", "canonical": "rgba(36, 50, 70, 0.62)", "note": "Translucent Field Ink tint per elevation tier (since 2026-09-22; before that GlassPanel was clear glass). On the TV the alpha follows the host panel-opacity setting." },
      "glass-surface-overlay": { "role": "glass-surface", "displayName": "Glass Surface Overlay", "canonical": "rgba(4, 6, 11, 0.85)", "note": "Translucent Field Ink tint per elevation tier (since 2026-09-22; before that GlassPanel was clear glass). On the TV the alpha follows the host panel-opacity setting. Darkest tier: a modal must dominate everything under it." },
      "glass-tint-pitch-button": { "role": "glass-surface", "displayName": "Pitch Glass Tint (primary button)", "canonical": "rgba(121, 143, 70, 0.45)", "note": "deriveGlassTint(Territory Green, 0.45) — the primary button surface." },
      "glass-tint-pitch-kicker": { "role": "glass-surface", "displayName": "Pitch Glass Tint (board kicker)", "canonical": "rgba(121, 143, 70, 0.16)", "note": "deriveGlassTint(Territory Green, 0.16) — the TV instruction kicker above the board." }
    },
    "typographyMeta": {
      "display": { "displayName": "Display", "purpose": "Hero numerals, big scores, phase headlines. Lexend via the swappable 'Brand Font' @font-face alias." },
      "h1": { "displayName": "H1", "purpose": "Screen/page titles." },
      "body": { "displayName": "Body", "purpose": "Default UI copy. Gilroy via the swappable 'Body Font' @font-face alias." },
      "label": { "displayName": "Label", "purpose": "Kickers, badges, meta text. Always uppercase, 16px extrabold, 0.1em tracking." },
      "mono": { "displayName": "Mono", "purpose": "Tabular data: dice rolls, codes." }
    },
    "shadows": [
      { "name": "pitch-glow", "value": "0 8px 22px color-mix(in srgb, #84ad28 35%, transparent)", "purpose": "The single primary-CTA glow, composited as the primary button's --glass-shadow over its pitch-tinted glass surface. Reserved for Button primary only." },
      { "name": "glass-base", "value": "0 8px 24px -10px rgba(0, 0, 0, 0.50)", "purpose": "Default GlassPanel/Button elevation shadow (side panels, secondary actions)." },
      { "name": "glass-raised", "value": "0 16px 40px -12px rgba(0, 0, 0, 0.55)", "purpose": "Active-player/CTA-block GlassPanel elevation shadow." },
      { "name": "glass-overlay", "value": "0 24px 64px -16px rgba(0, 0, 0, 0.65)", "purpose": "Modal/event-card GlassPanel elevation shadow — heaviest tier, detaches the modal from the entire TV stage." },
      { "name": "glass-recessed", "value": "inset 0 2px 6px rgba(0, 0, 0, 0.45)", "purpose": "Sunken/inactive glass panel shading." },
      { "name": "glass-inner-highlight", "value": "inset 0 1px 0 rgba(255, 255, 255, 0.16)", "purpose": "Top-edge bevel highlight on every glass surface, paired with glass-border." },
      { "name": "card", "value": "0 1px 2px rgba(10,14,23,.06), 0 4px 16px -6px rgba(10,14,23,.12)", "purpose": "Legacy flat-card shadow, superseded by the glass shadows above; retained for reference only." },
      { "name": "sheet", "value": "0 -8px 40px -12px rgba(10,14,23,.35)", "purpose": "Legacy bottom-sheet shadow, superseded by glass-overlay." },
      { "name": "glow-live", "value": "0 0 0 1px rgba(216,38,43,.5), 0 6px 24px -8px rgba(216,38,43,.45)", "purpose": "Live/urgent-state emphasis." },
      { "name": "glass-text-shadow", "value": "0 1px 3px rgba(4, 6, 11, 0.55)", "purpose": "Contrast floor for text rendered directly on ANY glass surface (base/raised/overlay, no exceptions) — since glass carries no background tint of its own, this shadow plus the --glass-fg-* opacity steps are the entire legibility mechanism. Same dark ink triplet as --on-pitch/the stage scrim, not a new color. See The On-Glass Text Rule." }
    ],
    "motion": [
      { "name": "atlas-ease", "value": "cubic-bezier(.2,.7,.3,1)", "purpose": "Default entrance/reveal easing used across nearly every TV and phone animation (chip-in, card reveal, overlay-in, badge swap)." },
      { "name": "atlas-ease-brisk", "value": "cubic-bezier(.2,.8,.3,1)", "purpose": "Slightly snappier variant used for dice-roll/tumble animations." },
      { "name": "atlas-duration-reveal", "value": "300ms-500ms", "purpose": "Typical duration band for state-driven reveals (chip-in, panel-in, stage-in)." },
      { "name": "atlas-overlay-in", "value": "atlasOverlayIn .3s ease-out both", "purpose": "Stage-scrim crossfade-in when the persistent illustration background changes phase level." },
      { "name": "atlas-overlay-out", "value": "atlasOverlayOut .32s ease-out both", "purpose": "Stage-scrim crossfade-out counterpart to atlas-overlay-in." },
      { "name": "ticker-crawl", "value": "atlasTicker, linear, infinite, 85px/s", "purpose": "The TV \"Verloop\" ticker: a doubled band moved translateX(0 → -50%) at a constant 85px/s, so the duration follows the band width. The only continuous linear motion in the system; still under reduced motion." }
    ]
  },
  "components": [
    {
      "name": "Glass Panel",
      "kind": "card",
      "refersTo": "glass-panel-base",
      "description": "Shared dark-tinted glass surface primitive: tier tint + blur (saturate 1.0) + hairline border + inner highlight + tier shadow. Elevation (base/raised/overlay: tint alpha, blur, shadow) and context (tv/phone blur scale) compose independently; nested panels keep their tint but drop their own backdrop-filter. On the TV, tint alpha and blur follow the host display settings.",
      "html": "<div class=\"ds-glass-panel\">GEBIED: NOORD-AFRIKA</div>",
      "css": ".ds-glass-panel { border-radius: 16px; padding: 16px; background: rgba(20, 29, 44, 0.72); border: 1px solid rgba(255,255,255,0.14); box-shadow: 0 8px 24px -10px rgba(0,0,0,0.50), inset 0 1px 0 rgba(255,255,255,0.16); backdrop-filter: blur(32px) saturate(1); -webkit-backdrop-filter: blur(32px) saturate(1); color: #eef2f8; text-shadow: 0 2px 6px rgba(4,6,11,0.85); font-family: 'Gilroy', sans-serif; }"
    },
    {
      "name": "Primary Button",
      "kind": "button",
      "refersTo": "button-primary",
      "description": "Full-width CTA (Start spel, Gooien, confirm actions) on pitch-tinted glass plus the system’s one signature glow.",
      "html": "<button class=\"ds-btn-primary\">START SPEL</button>",
      "css": ".ds-btn-primary { display: flex; align-items: center; justify-content: center; min-height: 64px; width: 100%; border-radius: 16px; background: rgba(121, 143, 70, 0.45); backdrop-filter: blur(16px) saturate(1); -webkit-backdrop-filter: blur(16px) saturate(1); color: #eef2f8; text-shadow: 0 2px 6px rgba(4,6,11,0.85); font-family: 'Lexend', sans-serif; font-weight: 800; font-size: 18px; border: 1px solid rgba(255,255,255,0.14); box-shadow: 0 8px 22px rgba(132,173,40,0.35), inset 0 1px 0 rgba(255,255,255,0.16); } .ds-btn-primary:focus-visible { outline: 2px solid #6ba2d8; outline-offset: 2px; } .ds-btn-primary:disabled { cursor: not-allowed; opacity: 0.5; }"
    },
    {
      "name": "Secondary Button",
      "kind": "button",
      "refersTo": "button-secondary",
      "description": "Quiet, non-glowing action on the neutral Base glass tint with a border-strong outline — e.g. \"TV-weergave\", \"Sluiten\", \"Standaard\". Sits above the primary when both stack.",
      "html": "<button class=\"ds-btn-secondary\">ANNULEREN</button>",
      "css": ".ds-btn-secondary { display: flex; align-items: center; justify-content: center; min-height: 64px; width: 100%; border-radius: 16px; background: rgba(20, 29, 44, 0.72); backdrop-filter: blur(16px) saturate(1); -webkit-backdrop-filter: blur(16px) saturate(1); color: #eef2f8; text-shadow: 0 2px 6px rgba(4,6,11,0.85); border: 1px solid #33425a; font-family: 'Lexend', sans-serif; font-weight: 800; font-size: 18px; box-shadow: inset 0 1px 0 rgba(255,255,255,0.16); } .ds-btn-secondary:focus-visible { outline: 2px solid #6ba2d8; outline-offset: 2px; } .ds-btn-secondary:disabled { cursor: not-allowed; opacity: 0.5; }"
    },
    {
      "name": "Text Field",
      "kind": "input",
      "refersTo": "input-field",
      "description": "Silver-bordered text input, e.g. join-code entry.",
      "html": "<input class=\"ds-input\" placeholder=\"CODE\" />",
      "css": ".ds-input { border-radius: 10px; border: 1px solid #627798; background: rgba(255,255,255,0.05); padding: 16px; font-family: 'Lexend', sans-serif; font-size: 17px; font-weight: 700; color: #eef2f8; } .ds-input:focus-visible { outline: 2px solid #4f8fd4; outline-offset: 2px; }"
    },
    {
      "name": "Silver Outline Badge",
      "kind": "chip",
      "refersTo": "badge-silver-outline",
      "description": "Default neutral kicker/phase-label chip.",
      "html": "<span class=\"ds-badge-silver\">CLAIMEN</span>",
      "css": ".ds-badge-silver { display: inline-flex; padding: 6px 16px; border-radius: 9999px; border: 1px solid #4f607d; color: #9cb0ca; font-family: 'Gilroy', sans-serif; font-size: 11px; font-weight: 800; letter-spacing: 0.08em; text-transform: uppercase; }"
    },
    {
      "name": "Pitch Solid Badge",
      "kind": "chip",
      "refersTo": "badge-pitch-solid",
      "description": "Confirmed/active-state chip variant.",
      "html": "<span class=\"ds-badge-pitch\">ACTIEF</span>",
      "css": ".ds-badge-pitch { display: inline-flex; padding: 6px 16px; border-radius: 9999px; background: #a1c23a; color: #04060b; font-family: 'Gilroy', sans-serif; font-size: 11px; font-weight: 800; letter-spacing: 0.08em; text-transform: uppercase; }"
    },
    {
      "name": "Dice",
      "kind": "custom",
      "description": "Seat-colored die: its own glass surface (dedicated 12px blur base, not a GlassPanel elevation tier), a two-stop 135° seat-color gradient fill, a 60%-alpha seat-color border, a fixed top-left light shadow + slight perspective tilt, and recessed (not embossed) pips with a seat-colored outer glow. On the TV it scales as a whole with the host dice-size setting; the border never drops below 1px.",
      "html": "<div class=\"ds-dice\"><span class=\"ds-dice-pip\"></span></div>",
      "css": ".ds-dice { display: grid; place-items: center; width: 56px; height: 56px; border-radius: 10px; background: linear-gradient(135deg, rgba(166,48,78,0.45), rgba(166,48,78,0.28)); border: 1px solid color-mix(in srgb, #a6304e 60%, transparent); box-shadow: inset 1px 1px 0 rgba(255,255,255,.45), inset -1px -1px 0 rgba(0,0,0,.25), inset 0 3px 0 rgba(255,255,255,.18), 0 8px 20px -6px rgba(0,0,0,.45); backdrop-filter: blur(12px) saturate(1); -webkit-backdrop-filter: blur(12px) saturate(1); transform: perspective(400px) rotateX(5deg); } .ds-dice-pip { width: 14px; height: 14px; border-radius: 9999px; background: radial-gradient(circle at 35% 35%, rgba(255,255,255,.95), rgba(255,255,255,.7)), #eef2f8; box-shadow: inset 0 -1px 2px rgba(0,0,0,.35), 0 1px 0 rgba(255,255,255,.3), 0 0 6px rgba(166,48,78,0.6); }"
    },
    {
      "name": "Selectable Option",
      "kind": "custom",
      "description": "Card whose border color alone carries selected/unselected/disabled state — no checkmark or icon overlay. Defaults to role=\"radio\" for mutually-exclusive groups (color pickers, role lists, territory/army selection); role=\"checkbox\" switches the same visual pattern to independent multi-select (e.g. choosing cards to trade in) without changing the border/background language.",
      "html": "<button class=\"ds-selectable\" role=\"radio\" aria-checked=\"true\">GROEN</button>",
      "css": ".ds-selectable { position: relative; border-radius: 16px; border-width: 2px; border-style: solid; border-color: #84ad28; padding: 16px; color: #eef2f8; font-family: 'Lexend', sans-serif; font-weight: 700; background: transparent; } .ds-selectable[aria-checked=\"false\"] { border-color: #33425a; } .ds-selectable:disabled { border-color: #243246; opacity: 0.5; cursor: not-allowed; }"
    },
    {
      "name": "Territory Card Tile",
      "kind": "custom",
      "refersTo": "glass-panel-raised",
      "description": "One territory card, three centered parts with no dividers between them (revised 2026-09-17 after the user rejected an earlier value/name/outline, hairline-divided build on sight): the territory name (H3), a normalized outline drawing of the territory derived from the map's own GeoJSON, and a filled value icon + uppercase symbol label. The outline part carries double the flex share of the other two and its drawing is unclamped, filling that space fully (revised again 2026-09-17: \"de kaarten iets te klein\"). Height follows the tile's own content — a fixed aspect ratio was tried and explicitly removed 2026-09-18 (\"actively making the UI worse\"). Only when the card is one of the player's own territories, a Territory-Green border signals ownership (2026-09-18: the earlier 'Gebied in bezit' text line under it was deliberately removed in the same feedback round, border only now) — no second color, dot, or bonus amount. A joker fills all three parts with value icons instead, in symbol order, plus the same uppercase symbol-label treatment as a regular card pinned to the bottom of the tile reading 'Joker'. Value icons render as one merged fill-rule=evenodd path per icon (all source subpaths baked to absolute coordinates and combined) so internal holes — a wheel hub, a belt buckle — actually punch through instead of being masked by an overlapping opaque fill from a separate path. Passive in a hand-overview grid, or wrapped as the body of a Selectable Option (role=checkbox) in a trade-selection grid.",
      "html": "<div class=\"ds-card-tile ds-card-tile--owned\"><div class=\"ds-card-tile-part\"><span class=\"ds-card-tile-name\">Alaska</span></div><div class=\"ds-card-tile-part ds-card-tile-part--outline\"><svg class=\"ds-card-tile-outline\" viewBox=\"0 0 64 64\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.5\" stroke-linejoin=\"round\" aria-hidden=\"true\"><path d=\"M8 40 L20 10 L44 8 L58 28 L48 56 L18 52 Z\"/></svg></div><div class=\"ds-card-tile-part\"><svg class=\"ds-card-tile-icon\" viewBox=\"0 0 24 32\" fill=\"currentColor\" fill-rule=\"evenodd\" aria-hidden=\"true\"><path d=\"M12 0c2.8 0 5 2.2 5 5s-2.2 5-5 5-5-2.2-5-5 2.2-5 5-5zM6 12h12l3 8-4 1-1-4-1 15H9L8 17l-1 4-4-1zM12 14.5a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3z\"/></svg><span class=\"ds-card-tile-symbol\">INFANTERIE</span></div></div>",
      "css": ".ds-card-tile { display: flex; flex-direction: column; overflow: hidden; border-radius: 16px; background: transparent; border: 1px solid rgba(255,255,255,0.14); box-shadow: 0 16px 40px -12px rgba(0,0,0,0.55), inset 0 1px 0 rgba(255,255,255,0.16); backdrop-filter: blur(16px) saturate(1.4); -webkit-backdrop-filter: blur(16px) saturate(1.4); text-align: center; } .ds-card-tile--owned { border-color: #536f1b; } .ds-card-tile-part { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 4px; padding: 4px 8px; } .ds-card-tile-part--outline { flex: 2; } .ds-card-tile-icon { width: 96px; height: 96px; color: #eef2f8; } .ds-card-tile-symbol { font-family: 'Gilroy', sans-serif; font-size: 11px; font-weight: 800; letter-spacing: 0.12em; text-transform: uppercase; color: color-mix(in srgb, #eef2f8 60%, transparent); } .ds-card-tile-name { font-family: 'Lexend', sans-serif; font-size: 17px; font-weight: 600; color: #eef2f8; text-shadow: 0 1px 3px rgba(4,6,11,0.55); } .ds-card-tile-outline { width: 100%; height: 100%; color: #eef2f8; }"
    },
    {
      "name": "Slider",
      "kind": "input",
      "refersTo": "slider",
      "description": "Native range input for the TV display settings: label + tabular value on one row, the track colored only via accent-color (Territory Green). Commits once on release; snaps back if the server refuses.",
      "html": "<label class=\"ds-slider\"><span class=\"ds-slider-row\"><span class=\"ds-slider-label\">Tekstgrootte</span><span class=\"ds-slider-value\">50%</span></span><input class=\"ds-slider-input\" type=\"range\" min=\"0\" max=\"100\" step=\"5\" value=\"50\"></label>",
      "css": ".ds-slider { display: flex; flex-direction: column; gap: 6px; font-family: 'Gilroy', sans-serif; } .ds-slider-row { display: flex; align-items: baseline; justify-content: space-between; gap: 12px; } .ds-slider-label { font-size: 15px; color: color-mix(in srgb, #eef2f8 90%, transparent); text-shadow: 0 2px 6px rgba(4,6,11,0.85); } .ds-slider-value { font-family: 'Lexend', sans-serif; font-size: 17px; font-weight: 800; font-variant-numeric: tabular-nums; color: #eef2f8; text-shadow: 0 2px 6px rgba(4,6,11,0.85); } .ds-slider-input { width: 100%; min-height: 52px; accent-color: #84ad28; } .ds-slider-input:focus-visible { outline: 2px solid #6ba2d8; outline-offset: 2px; } .ds-slider-input:disabled { cursor: not-allowed; opacity: 0.5; }"
    },
    {
      "name": "TV Display Panel Section",
      "kind": "card",
      "description": "One section of the host-only \"TV-weergave\" panel: an uppercase kicker over a raised glass block holding Sliders (Scherm, Dobbelstenen) or the NL/EN segmented control (Taal op de TV).",
      "html": "<section class=\"ds-tv-section\"><span class=\"ds-tv-kicker\">DOBBELSTENEN</span><label class=\"ds-tv-row\"><span>Grootte van de dobbelstenen</span><strong>50%</strong></label><input class=\"ds-tv-range\" type=\"range\" min=\"0\" max=\"100\" step=\"5\" value=\"50\"></section>",
      "css": ".ds-tv-section { display: flex; flex-direction: column; gap: 12px; padding: 16px; border-radius: 16px; background: rgba(36, 50, 70, 0.62); border: 1px solid rgba(255,255,255,0.14); box-shadow: 0 16px 40px -12px rgba(0,0,0,0.55), inset 0 1px 0 rgba(255,255,255,0.16); backdrop-filter: blur(16px) saturate(1); -webkit-backdrop-filter: blur(16px) saturate(1); font-family: 'Gilroy', sans-serif; } .ds-tv-kicker { font-size: 11px; font-weight: 800; letter-spacing: 0.12em; text-transform: uppercase; color: color-mix(in srgb, #eef2f8 75%, transparent); text-shadow: 0 2px 6px rgba(4,6,11,0.85); } .ds-tv-row { display: flex; justify-content: space-between; align-items: baseline; font-size: 15px; color: #eef2f8; text-shadow: 0 2px 6px rgba(4,6,11,0.85); } .ds-tv-row strong { font-family: 'Lexend', sans-serif; font-size: 17px; font-weight: 800; font-variant-numeric: tabular-nums; } .ds-tv-range { width: 100%; min-height: 52px; accent-color: #84ad28; }"
    },
    {
      "name": "Action Ticker",
      "kind": "card",
      "description": "TV \"Verloop\": the last actions (up to 10, newest first) as one right-to-left news ticker in the bottom row of the board grid. Base glass frame with a Label kicker; items are --atlas-row pills with the seat avatar, the actor name in extrabold on-glass text and one H3-size sentence; only the newest carries a silver-outline \"Laatste\" badge. The band is rendered twice and moved with atlasTicker (translateX 0 → -50%, linear, infinite) at 85px/s; it stands still under reduced motion.",
      "html": "<section class=\"ds-ticker\"><span class=\"ds-ticker-kicker\">VERLOOP</span><div class=\"ds-ticker-band\"><div class=\"ds-ticker-item\"><span class=\"ds-ticker-badge\">LAATSTE</span><span class=\"ds-ticker-avatar\"></span><span class=\"ds-ticker-text\"><b>Alice</b> plaatst 3 legers op Brazilië. Totaal nu 8.</span></div><div class=\"ds-ticker-item\"><span class=\"ds-ticker-avatar\"></span><span class=\"ds-ticker-text\"><b>Bob</b> claimt Peru</span></div></div></section>",
      "css": ".ds-ticker { display: flex; flex-direction: column; padding: 12px 18px; border-radius: 16px; background: rgba(20, 29, 44, 0.72); border: 1px solid rgba(255,255,255,0.14); box-shadow: 0 8px 24px -10px rgba(0,0,0,0.50), inset 0 1px 0 rgba(255,255,255,0.16); backdrop-filter: blur(32px) saturate(1); -webkit-backdrop-filter: blur(32px) saturate(1); overflow: hidden; font-family: 'Gilroy', sans-serif; } .ds-ticker-kicker { margin-bottom: 8px; font-size: 16px; font-weight: 800; letter-spacing: 0.1em; text-transform: uppercase; color: color-mix(in srgb, #eef2f8 75%, transparent); text-shadow: 0 2px 6px rgba(4,6,11,0.85); } .ds-ticker-band { display: flex; gap: 12px; width: max-content; animation: atlasTicker 20s linear infinite; } .ds-ticker-item { display: flex; flex: none; align-items: center; gap: 11px; padding: 11px 13px; border-radius: 12px; background: rgba(255,255,255,.035); border: 1px solid #243044; } .ds-ticker-badge { padding: 6px 16px; border-radius: 999px; border: 1px solid #627798; color: #c2cddd; font-size: 11px; font-weight: 800; letter-spacing: 0.025em; } .ds-ticker-avatar { width: 34px; height: 34px; border-radius: 9px; background: #ca3c25; } .ds-ticker-text { white-space: nowrap; font-size: 17px; line-height: 1.25; color: #eef2f8; text-shadow: 0 2px 6px rgba(4,6,11,0.85); } @keyframes atlasTicker { from { transform: translateX(0); } to { transform: translateX(-50%); } } @media (prefers-reduced-motion: reduce) { .ds-ticker-band { animation: none; } }"
    }
  ],
  "narrative": {
    "northStar": "Frosted Front Line",
    "overview": "Operatie Atlas puts a shared TV screen at the center of the room and hands every player a private phone controller. The system's original framing — sports broadcast graphics (scoreboards, lower-thirds, on-screen tickers) rather than board-game skeuomorphism — still holds for type, color, and motion, but the surface material has evolved: every TV and phone shell now renders a persistent, full-bleed war-room illustration (commanders around a map table, a lit battlefield) behind the UI, and every card, panel, button, and modal is a pane of dark-tinted glass set over it (GlassPanel, and Button's own glass surface): a translucent Field Ink tint per elevation tier (glass-surface-*), a heavy backdrop-filter: blur() with no saturation boost, a hairline border, an inner highlight, and a shadow. Until 2026-09-22 this was *clear* glass — no tint at all, the apparent color entirely whatever was blurred behind it. Viewed on a real TV from across a room, that let the busy illustration read as noise behind text, so the tint and a doubled blur were added deliberately as a legibility floor: the glass now calms what's behind it rather than just distorting it. The room the player is standing in didn't change; the walls did, and they're smoked glass now, not clear. Legibility is still the reason for every bold choice — black-weight display type still has to read at distance, tabular numerals still exist so a glanced-at number registers instantly — and it rests on three layers together: the tint itself, a shared text-shadow plus opacity-stepped light text (see The On-Glass Text Rule), and the shell's own stage scrim sitting behind everything (see Layout). On the TV, the host can tune tint, blur, type and dice size per game from their phone (see Layout → TV display settings). The mood follows from that constraint, not from an aesthetic preference: contrast is spent only where state needs to register from across a room, and everywhere else the design steps back. This is the same instinct as The Invisible Design Rule (see Named Rules, below): the goal isn't confidence for its own sake, it's a screen where the content — whose turn, what changed, what's tappable — is the only thing that's loud. Glass earns its place under that same rule: a GlassPanel never nests inside another GlassPanel with its own backdrop-filter (a shared React context forces the inner one flat), because a second blur pass would just be cost without a legible difference — restraint that reads as one continuous glass-and-light system, not repeated decoration. Both the TV and phone shells force dark mode regardless of the device's OS theme preference — this is a deliberate, hard invariant (TvShell/PhoneShell both apply a dark class unconditionally), because the product is built to be read across a room on a TV, and a light system tray theme has no bearing on that. A complete light theme exists in the token layer in parallel (see Light theme, below) for any surface that isn't the TV/phone game shells themselves; the glass system itself is dark-only today (glass-tokens.ts — no light-mode glass surface fallback colors exist yet).",
    "keyCharacteristics": [
      "Black-weight (800), display-font headlines and scores; body copy stays lighter and calmer.",
      "Dark-forced game shells; a parallel light theme exists in tokens but isn't wired to any current screen, and doesn't extend to the glass layer.",
      "A persistent, full-bleed illustration + directional scrim sits behind every TV and phone screen, crossfading its intensity per phase — see Layout, below.",
      "Nearly every surface — cards, panels, buttons, modals — is dark-tinted glass (translucent Field Ink tint + heavy blur + hairline border + inner highlight + shadow) floating over that illustration, not a flat opaque layer.",
      "On the TV, the host can scale type (including map markers), glass tint/blur and dice from their phone; 50% is always exactly this design (see Layout → TV display settings).",
      "The primary CTA still carries the system's one signature glow, now composited as the --glass-shadow on top of its glass surface rather than a shadow on a flat fill.",
      "Uppercase, wide-tracked eyebrow/label text throughout (kickers, badges, stat headers).",
      "Tabular numerals everywhere a value could change in place (timers, scores, dice results).",
      "Seven colorblind-safe player seat colors (data/colors.json) sit *outside* this brand palette — see Player seat colors, below. A derived player.glass.*/player.diceFace.* layer exists specifically for translucent player-color surfaces (dice faces, selection tints); see Player seat colors."
    ],
    "rules": [
      { "name": "The Invisible Design Rule", "body": "If a screen reads just as clearly with an element removed, remove it. Chrome — borders, glow, extra type weight, motion — only exists to carry game state (whose turn, what changed, what's tappable); it never exists to look finished. The One Glow Rule and the Flat-By-Default Rule are both instances of this: when two solutions communicate the same state equally well, ship the plainer one.", "section": "colors" },
      { "name": "The Tabular Numerals Rule", "body": "Any number that can change in place — timers, dice results, army counts, scores — uses font-variant-numeric: tabular-nums so digit width never shifts and adjacent UI doesn't reflow as the value updates.", "section": "typography" },
      { "name": "The On-Glass Text Rule", "body": "Text rendered directly on any GlassPanel tier (or the glass Button) never uses the standard --fg/--fg-secondary/--fg-muted gray-scale — that scale was tuned for a flat, opaque card, and a translucent tint over a busy photo still lets a mid-tone gray lose against its brightest patches no matter how far up the scale it sits. Instead, on-glass text stays on one light color (--fg1/#eef2f8) at three opacity steps — --glass-fg (100%), --glass-fg-secondary (90%), --glass-fg-muted (75%) — carrying hierarchy through opacity, not hue-lightness, paired with a shared dark text-shadow (0 2px 6px rgba(4,6,11,.85), the same ink triplet as --on-pitch/the stage scrim) that supplies a contrast floor regardless of what's behind the glass. (Raised from 80/60% and a lighter 0 1px 3px .55 shadow on 2026-09-22, together with the glass tint, after viewing on a real TV at ~3m.) Applied automatically in CSS to any .text-fg/.text-fg-secondary/.text-fg-muted descendant of a filtering glass surface (index.css) — never opted into per component, and never limited to a subset of tiers. Text on non-glass, opaque surfaces is unaffected and keeps the standard --fg/--fg-secondary/--fg-muted scale.", "section": "typography" },
      { "name": "The Scalable Type Rule", "body": "On the TV, every size comes from the type scale — the six named steps or the numbered size1–size14 (text-h1, text-size5, …) — never Tailwind's own text-lg/text-xl/text-2xl or a literal text-[Npx]. The host's text-size setting overrides exactly those --text-* variables inside TvShell (see Layout → TV display settings); anything sized outside the scale silently stays put while the rest of the screen grows. Five such call sites were moved onto their pixel-equal steps on 2026-09-24 (18/20/24px → size2/size4/size5), keeping Tailwind's own line-height literally where no leading-* was set, so nothing shifted at the default setting.", "section": "typography" },
      { "name": "The Glass-By-Default Rule", "body": "Raised surfaces are glass at rest: tier tint, blur, hairline border, inner highlight, and a tier-appropriate shadow, together — never a flat opaque wash, never a shadow alone, and never a hand-picked tint (only the tier tints or a deriveGlassTint result — see Colors → Glass surfaces). A surface only goes flat/opaque under one of the three fallback conditions above; that fallback is an accessibility/compatibility floor, not a second style to reach for by choice.", "section": "elevation" },
      { "name": "The One Glow Rule", "body": "The glow shadow (shadow-glow-pitch) is reserved for the single primary CTA on a screen, now expressed as that button's --glass-shadow. It is a scarcity signal riding on top of the glass system, not a separate elevation tier.", "section": "elevation" },
      { "name": "The No-Nested-Blur Rule", "body": "A GlassPanel (or glass button) rendered inside another glass surface never applies its own backdrop-filter — the nesting context forces it flat/opaque. Stacking blur passes is a GPU cost with no legible benefit and a visible glass-on-glass artifact; the fix is structural (context guard), not a per-instance judgment call.", "section": "elevation" }
    ],
    "dos": [
      "Do build new raised surfaces on GlassPanel (or its shared CSS class, as Button does) rather than a flat tonal background — see The Glass-By-Default Rule.",
      "Do reserve the glow shadow (shadow-glow-pitch) for the single primary CTA on a screen, expressed as its --glass-shadow — it's a scarcity signal, not decoration.",
      "Do let a GlassPanel (or glass button) inside another glass surface render unblurred via the nesting context — never force a second backdrop-filter pass.",
      "Do size TV text from the type scale (text-h1…, text-size1…size14) so it follows the host's text-size setting — see The Scalable Type Rule.",
      "Do read useTvDisplayScale() for any TV element that draws its own glass, markers or dice outside GlassPanel/Dice, and scale it as a whole — including the layout that reserves room for it — so the host's settings reach it and 50% stays exactly the design.",
      "Do use the on-glass text treatment (--glass-fg-* opacity steps + shared text-shadow) for any text on any glass surface, including overlay, instead of the standard gray scale — see The On-Glass Text Rule.",
      "Do use tabular numerals (.tnum / font-variant-numeric: tabular-nums) for any value that updates in place.",
      "Do keep uppercase + wide letter-spacing (0.1em) for kicker/label/eyebrow text at 16px, extrabold weight.",
      "Do treat data/colors.json player seat colors as a separate system from this palette — never reuse a seat color as a UI brand color or vice versa; derive translucent seat-color surfaces via deriveGlassTint, never a hand-picked per-seat rgba literal.",
      "Do rely on the SelectableOption border/background change as the only selected-state signal — a redundant checkmark or icon on top of an already-distinct border is chrome that doesn't carry new information (The Invisible Design Rule).",
      "Do use role=\"checkbox\" on SelectableOption (instead of the radio default) whenever more than one tile can be selected at once — the visual pattern stays identical, only the accessible semantics change to match the actual selection behavior.",
      "Do signal a possession/ownership state (e.g. \"Gebied in bezit\" on an owned territory's card) with a single border-color change plus one text line, the same pattern TerritoryCardTile uses — not a second color, dot, or icon layered on top (The Invisible Design Rule).",
      "Do drop a phase-name kicker/eyebrow above a heading when the heading alone already states the action (\"Verdeel je legers\", \"Wie mag beginnen?\"); when the heading alone isn't a complete statement (e.g. a bare territory name), merge the kicker's words into the one heading line instead of stacking two lines."
    ],
    "donts": [
      "Don't reintroduce a gold/trophy accent color for UI chrome — Recon Silver replaced it deliberately (2026-08-04) because the \"trophy/World Cup\" association didn't fit a conquest game. Caution Amber (--warning) is the one exception, since it's a functional status color, not decoration.",
      "Don't reach for the legacy flat tonal overlay (--atlas-t0X) as a first choice for a new card/panel/row — that's the retired default; glass is. The opaque glass-surface-*-opaque fills exist only for the two defined fallback routes (unsupported browser, reduced transparency), never as a stylistic alternative.",
      "Don't apply your own backdrop-filter/blur value outside glassBlur's three steps (14/32/44px) or invent a new elevation tier beyond Base/Raised/Overlay — extend glass-tokens.ts, don't hardcode a one-off in a component. Dice's own DICE_GLASS_BLUR_BASE (see Components → Dice) is the one documented exception: a die is chip-scale, not a panel/card/modal, so it isn't an elevation tier and was never meant to share glassBlur.sm.",
      "Don't hand-pick a tint for a GlassPanel/glass Button — use the tier tint (glass-surface-base/-raised/-overlay) or a deriveGlassTint result (as the primary button and the board kicker do); a literal rgba() in a component drifts the moment the tiers are retuned.",
      "Don't size TV text with Tailwind's own text-lg/text-xl/text-2xl or a literal text-[Npx] — it won't follow the host's text-size setting (The Scalable Type Rule).",
      "Don't express a TV display factor as CSS calc()/min() inside a color or filter — compute it in JS; an invalid value there drops the whole declaration on older TV browsers.",
      "Don't try to fix on-glass legibility by picking a different/darker gray or by lightening one further up the standard --fg/--fg-secondary/--fg-muted scale — any mid-tone color loses against an arbitrary bright photo patch. Use the opacity-stepped --glass-fg-* + text-shadow treatment instead.",
      "Don't assume the light theme is unused/dead — it's a maintained half of the token system, just not wired to the TV/phone game shells, and the glass layer specifically has no light-mode tints at all today (a gap, not a design decision, if a light glass surface is ever needed).",
      "Don't use a decorative unicode emoji or glyph as a stand-in icon (🎲, ⚔, 👑, 📺, ⏱, ›, ◌, ✓). ColorSymbol's player-seat glyphs (▲ ● ■ ★ ✚ ⬡ ◆) are the one exception — they're colorblind-accessibility data sourced from frozen data/colors.json, not decoration standing in for missing UI (removed across the phone screens, 2026-08-05).",
      "Don't treat every icon as stroke=currentColor/fill=none — the 5 territory-card value icons (cardSymbolIcons.tsx, see Components → Territory Card Tile) are the one documented exception: filled silhouettes, user-supplied to match the physical reference card's solid-icon/thin-outline contrast, not a stylistic drift from the line-icon convention."
    ]
  }
}
