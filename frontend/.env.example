# Basis-URL van de Azure App Service-API (zonder trailing slash), bv.
# https://riskop-api.azurewebsites.net — alleen nodig voor productiebuilds op Vercel.
# Leeg/afwezig laten voor lokale dev (pnpm run dev): vite.config.ts's proxy handelt
# /hubs, /games en /maps dan zelf af richting de lokale API.
VITE_API_URL=
